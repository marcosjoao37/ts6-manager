# SAML SSO Authentication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un login SSO SAML 2.0 en option à côté du login local, avec provisioning JIT configurable et mapping de rôle depuis l'IdP.

**Architecture :** Flux SP-initiated. Le frontend redirige vers `/api/auth/saml/login` (AuthnRequest → IdP Authentik). L'IdP POST la SAMLResponse sur `/api/auth/saml/acs` ; le backend la valide (`@node-saml/node-saml`), résout/crée l'utilisateur, recalcule son rôle, crée un **code SSO à usage unique**, puis redirige vers `/login/sso?code=…`. Le frontend échange le code via `/api/auth/saml/exchange` contre une session (JWT + refresh) ou un challenge MFA — réutilisant le pipeline d'auth existant. La config IdP vit dans un modèle `SAMLSettings` éditable via l'UI admin.

**Tech Stack :** TypeScript 5.9 strict, Node 20+, Express 4, Prisma 6 + SQLite, `@node-saml/node-saml`, jsonwebtoken, React 18 + Vite (BrowserRouter), TanStack Query, Radix UI, react-i18next, vitest, pnpm workspace.

## Global Constraints

- Monorepo pnpm : `@ts6/backend`, `@ts6/frontend`, `@ts6/common`. Imports backend ESM avec extension `.js`.
- Convention repo : `generated/prisma` et `prisma/migrations` sont **gitignorés** ; ne commiter que `schema.prisma`. Migration via `pnpm --filter @ts6/backend exec prisma migrate dev --name <n>`. Si `migrate dev` réclame un reset de la base → STOP, ne pas accepter.
- Le conteneur applique le schéma au démarrage via `prisma db push` — pas besoin de commiter les migrations.
- Rôles : `'admin' | 'viewer'` (`packages/common/src/types/auth.ts`). `defaultRole` par défaut = `viewer`.
- Secrets stockés chiffrés via `encrypt`/`decrypt` de `packages/backend/src/utils/crypto.js` (AES-256-GCM), comme le `botToken` Discord.
- SAML endpoints publics montés **avant** `app.use('/api', authMiddleware)` dans `app.ts`. Endpoints admin des réglages montés **après**, avec `requireRole('admin')`.
- Handoff par **code SSO à usage unique** : jamais de token dans l'URL.
- Validation d'assertion obligatoire : signature (cert IdP), audience == `spEntityId`, fenêtre temporelle, anti-rejeu `InResponseTo`.
- i18n : toute clé UI nouvelle dans les **5 langues** FR/EN/DE/ES/IT.
- Hors périmètre v1 : SAML SLO, signature des AuthnRequests par le SP, allowlist par domaine/groupe.
- Défauts d'attributs ciblant Authentik (voir spec) ; tous éditables.
- Commandes : `pnpm --filter @ts6/backend exec tsc --noEmit`, `pnpm --filter @ts6/backend exec vitest run [chemin]`, `pnpm --filter @ts6/frontend exec tsc --noEmit`.

---

### Task 1: Schéma Prisma (User + SAMLSettings) + garde login local

Ajoute les champs SAML au modèle `User`, le modèle `SAMLSettings`, et — parce que rendre `passwordHash` nullable casse la compilation du login local — met à jour le login local pour rejeter un compte sans mot de passe. Livrable testable : compilation verte + login local sûr.

**Files:**
- Modify: `packages/backend/prisma/schema.prisma` (model `User` ~lignes 11-33 ; ajouter model `SAMLSettings`)
- Modify: `packages/backend/src/routes/auth.routes.ts` (handler `POST /login`, ~lignes 104-115)
- Test: `packages/backend/src/routes/auth-login-guard.test.ts` (créer)
- Generated (gitignorés, NE PAS commiter) : `generated/prisma/*`, `prisma/migrations/*`

**Interfaces:**
- Consumes: rien.
- Produces (types Prisma `User`) : `authProvider: string` (default `"local"`), `externalId: string | null`, `passwordHash: string | null`. Nouveau type `SAMLSettings` (voir champs ci-dessous). Contrainte : login local rejette `passwordHash == null`.

- [ ] **Step 1: Modifier le modèle `User`**

Dans `packages/backend/prisma/schema.prisma`, model `User` : rendre `passwordHash` nullable et ajouter les champs SAML + l'index unique.

```prisma
  passwordHash   String?
  // ... champs existants inchangés ...
  authProvider   String   @default("local") // "local" | "saml"
  externalId     String?                    // SAML nameID (stable key)

  @@unique([authProvider, externalId])
```

- [ ] **Step 2: Ajouter le modèle `SAMLSettings`**

À la fin du fichier (près de `AppSetting`/`DiscordSettings`) :

```prisma
model SAMLSettings {
  id              Int      @id @default(autoincrement())
  enabled         Boolean  @default(false)
  idpMetadataUrl  String?
  idpMetadataXml  String?  // encrypted
  idpEntityId     String?
  idpSsoUrl       String?
  idpCertificate  String?  // encrypted (PEM/base64)
  spEntityId      String?
  autoProvision   Boolean  @default(true)
  defaultRole     String   @default("viewer")
  attrUsername    String   @default("http://schemas.goauthentik.io/2021/02/saml/username")
  attrEmail       String   @default("http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress")
  attrDisplayName String   @default("http://schemas.goauthentik.io/2021/02/saml/displayname")
  attrRole        String?
  roleAdminValue  String?
  updatedAt       DateTime @updatedAt
}
```

- [ ] **Step 3: Générer migration + client**

Run: `pnpm --filter @ts6/backend exec prisma migrate dev --name add_saml_sso`
Expected: migration créée (locale, gitignorée), client régénéré, exit 0. (Si reset réclamé → STOP.)

- [ ] **Step 4: Écrire le test de garde du login local**

Créer `packages/backend/src/routes/auth-login-guard.test.ts`. Ce test vérifie la fonction pure de garde qu'on introduit à l'étape suivante :

```ts
import { describe, it, expect } from 'vitest';
import { canLocalLogin } from './auth.routes.js';

describe('canLocalLogin', () => {
  it('refuse un compte sans passwordHash (compte SAML)', () => {
    expect(canLocalLogin({ enabled: true, passwordHash: null })).toBe(false);
  });
  it('refuse un compte désactivé', () => {
    expect(canLocalLogin({ enabled: false, passwordHash: 'x' })).toBe(false);
  });
  it('autorise un compte local activé avec hash', () => {
    expect(canLocalLogin({ enabled: true, passwordHash: 'x' })).toBe(true);
  });
});
```

- [ ] **Step 5: Vérifier l'échec**

Run: `pnpm --filter @ts6/backend exec vitest run src/routes/auth-login-guard.test.ts`
Expected: FAIL (`canLocalLogin` non exporté).

- [ ] **Step 6: Implémenter la garde et l'utiliser dans `/login`**

Dans `packages/backend/src/routes/auth.routes.ts`, ajouter la fonction exportée (près du haut, après les imports) :

```ts
/** A user can attempt local password login only if enabled and has a local password. */
export function canLocalLogin(user: { enabled: boolean; passwordHash: string | null } | null): boolean {
  return !!user && user.enabled && !!user.passwordHash;
}
```

Puis remplacer le bloc de vérification (actuellement lignes ~106-115) par :

```ts
    if (!canLocalLogin(user)) {
      journal?.recordWebLogin(String(username), req.ip || '', false);
      throw new AppError(401, 'Invalid credentials');
    }

    const valid = await bcrypt.compare(password, user.passwordHash as string);
    if (!valid) {
      journal?.recordWebLogin(user.username, req.ip || '', false);
      throw new AppError(401, 'Invalid credentials');
    }
```

- [ ] **Step 7: Vérifier succès + compilation**

Run: `pnpm --filter @ts6/backend exec vitest run src/routes/auth-login-guard.test.ts && pnpm --filter @ts6/backend exec tsc --noEmit`
Expected: PASS (3 tests) + tsc clean.

- [ ] **Step 8: Commit**

```bash
git add packages/backend/prisma/schema.prisma packages/backend/src/routes/auth.routes.ts packages/backend/src/routes/auth-login-guard.test.ts
git commit -m "feat(auth): SAML user/settings schema + reject local login without password"
```
(migrations et `generated/prisma` gitignorés — rien d'autre à ajouter.)

---

### Task 2: Extraire les helpers de session

Déplace `issueSession`, `gateAfterPassword` et `signMfaChallenge` de `auth.routes.ts` vers un module partagé, pour que les routes SAML les réutilisent sans dupliquer la logique. Refactor sans changement de comportement.

**Files:**
- Create: `packages/backend/src/auth/session.ts`
- Modify: `packages/backend/src/routes/auth.routes.ts` (retirer les définitions, importer depuis le module)

**Interfaces:**
- Consumes: `config`, `jsonwebtoken`, `crypto`, `nanoid`.
- Produces:
  - `export async function issueSession(prisma: any, user: any): Promise<{ accessToken: string; refreshToken: string; user: { id: number; username: string; displayName: string; role: string; language: string | null } }>`
  - `export function signMfaChallenge(userId: number): string`
  - `export async function gateAfterPassword(prisma: any, user: any): Promise<{ accessToken: string; refreshToken: string; user: any } | { mfaRequired: true; mfaToken: string } | { mfaSetupRequired: true; mfaToken: string }>`

- [ ] **Step 1: Créer `session.ts` avec le code déplacé (identique)**

Créer `packages/backend/src/auth/session.ts` :

```ts
import jwt from 'jsonwebtoken';
import crypto from 'crypto';
import { nanoid } from 'nanoid';
import { config } from '../config.js';

const MFA_CHALLENGE_TTL = '5m';

export function signMfaChallenge(userId: number): string {
  return jwt.sign({ mfa: true, id: userId }, config.jwtSecret, { expiresIn: MFA_CHALLENGE_TTL } as jwt.SignOptions);
}

/** Issue access + refresh tokens and the user payload (shared by local login, MFA step and SAML). */
export async function issueSession(prisma: any, user: any) {
  const payload = { id: user.id, username: user.username, role: user.role };
  const accessToken = jwt.sign(payload, config.jwtSecret, { expiresIn: config.jwtAccessExpiry } as jwt.SignOptions);
  const refreshToken = crypto.randomBytes(64).toString('hex');
  const family = nanoid();
  const expiresAt = new Date();
  expiresAt.setDate(expiresAt.getDate() + 7);

  await prisma.refreshToken.create({ data: { token: refreshToken, userId: user.id, expiresAt, family } });
  await prisma.user.update({ where: { id: user.id }, data: { lastLoginAt: new Date() } });

  return {
    accessToken,
    refreshToken,
    user: { id: user.id, username: user.username, displayName: user.displayName, role: user.role, language: user.language },
  };
}

/**
 * Gating applied once the identity is proven (password or SAML assertion):
 * require the MFA second factor / enrollment, otherwise issue the session.
 */
export async function gateAfterPassword(prisma: any, user: any) {
  if (user.mfaEnabled) return { mfaRequired: true, mfaToken: signMfaChallenge(user.id) };
  if (user.mfaRequired) return { mfaSetupRequired: true, mfaToken: signMfaChallenge(user.id) };
  return issueSession(prisma, user);
}
```

- [ ] **Step 2: Mettre à jour `auth.routes.ts`**

Supprimer les définitions locales de `MFA_CHALLENGE_TTL`, `signMfaChallenge`, `issueSession`, `gateAfterPassword` (lignes ~19-23, 30-47, 62-67). Garder `verifyMfaChallenge` (utilisé par `/login/mfa`). Ajouter l'import en tête :

```ts
import { issueSession, gateAfterPassword, signMfaChallenge } from '../auth/session.js';
```

Vérifier que `verifyMfaChallenge` reste défini localement (il n'est pas déplacé). Les autres usages de `signMfaChallenge`/`issueSession`/`gateAfterPassword` pointent désormais vers l'import.

- [ ] **Step 3: Compiler + suite d'auth**

Run: `pnpm --filter @ts6/backend exec tsc --noEmit && pnpm --filter @ts6/backend exec vitest run`
Expected: PASS (compilation clean ; toute la suite existante toujours verte — comportement inchangé).

- [ ] **Step 4: Commit**

```bash
git add packages/backend/src/auth/session.ts packages/backend/src/routes/auth.routes.ts
git commit -m "refactor(auth): extract session/MFA-gate helpers into auth/session.ts"
```

---

### Task 3: Logique SAML pure (profil, rôle, désambiguïsation)

Fonctions pures et 100 % testables pour extraire un profil depuis les attributs d'assertion, calculer le rôle, et désambiguïser un username. Aucune dépendance DB/réseau.

**Files:**
- Create: `packages/backend/src/auth/saml/saml-user.ts`
- Test: `packages/backend/src/auth/saml/saml-user.test.ts`

**Interfaces:**
- Consumes: type `SAMLSettings` (Prisma).
- Produces:
  - `export interface SamlProfileInput { nameID: string; attributes: Record<string, unknown> }`
  - `export interface ResolvedSamlProfile { externalId: string; username: string; email: string | null; displayName: string; role: 'admin' | 'viewer' }`
  - `export function firstAttr(attributes: Record<string, unknown>, key: string): string | null`
  - `export function resolveRole(attributes: Record<string, unknown>, settings: { attrRole: string | null; roleAdminValue: string | null; defaultRole: string }): 'admin' | 'viewer'`
  - `export function buildSamlProfile(input: SamlProfileInput, settings: { attrUsername: string; attrEmail: string; attrDisplayName: string; attrRole: string | null; roleAdminValue: string | null; defaultRole: string }): ResolvedSamlProfile`
  - `export function disambiguateUsername(desired: string, isTaken: (candidate: string) => boolean): string`

- [ ] **Step 1: Écrire les tests**

Créer `packages/backend/src/auth/saml/saml-user.test.ts` :

```ts
import { describe, it, expect } from 'vitest';
import { firstAttr, resolveRole, buildSamlProfile, disambiguateUsername } from './saml-user.js';

const base = {
  attrUsername: 'user', attrEmail: 'email', attrDisplayName: 'dn',
  attrRole: 'groups', roleAdminValue: 'ts6-admins', defaultRole: 'viewer' as const,
};

describe('firstAttr', () => {
  it('retourne une valeur scalaire', () => {
    expect(firstAttr({ user: 'bob' }, 'user')).toBe('bob');
  });
  it('retourne le premier élément d’un tableau', () => {
    expect(firstAttr({ groups: ['a', 'b'] }, 'groups')).toBe('a');
  });
  it('retourne null si absent', () => {
    expect(firstAttr({}, 'user')).toBeNull();
  });
});

describe('resolveRole', () => {
  it('admin si une valeur du groupe == roleAdminValue', () => {
    expect(resolveRole({ groups: ['x', 'ts6-admins'] }, base)).toBe('admin');
  });
  it('defaultRole si pas de correspondance', () => {
    expect(resolveRole({ groups: ['x'] }, base)).toBe('viewer');
  });
  it('defaultRole si attrRole non configuré', () => {
    expect(resolveRole({ groups: ['ts6-admins'] }, { ...base, attrRole: null })).toBe('viewer');
  });
});

describe('buildSamlProfile', () => {
  it('mappe nameID + attributs', () => {
    const p = buildSamlProfile(
      { nameID: 'uid-1', attributes: { user: 'bob', email: 'bob@x.io', dn: 'Bob', groups: ['ts6-admins'] } },
      base,
    );
    expect(p).toEqual({ externalId: 'uid-1', username: 'bob', email: 'bob@x.io', displayName: 'Bob', role: 'admin' });
  });
  it('fallback username=email local-part puis nameID, displayName=username', () => {
    const p = buildSamlProfile({ nameID: 'uid-2', attributes: { email: 'jane@x.io' } }, base);
    expect(p.username).toBe('jane');
    expect(p.displayName).toBe('jane');
    expect(p.role).toBe('viewer');
  });
  it('username=nameID si ni username ni email', () => {
    const p = buildSamlProfile({ nameID: 'uid-3', attributes: {} }, base);
    expect(p.username).toBe('uid-3');
  });
});

describe('disambiguateUsername', () => {
  it('retourne tel quel si libre', () => {
    expect(disambiguateUsername('bob', () => false)).toBe('bob');
  });
  it('suffixe jusqu’à trouver un libre', () => {
    const taken = new Set(['bob', 'bob-2']);
    expect(disambiguateUsername('bob', (c) => taken.has(c))).toBe('bob-3');
  });
});
```

- [ ] **Step 2: Vérifier l'échec**

Run: `pnpm --filter @ts6/backend exec vitest run src/auth/saml/saml-user.test.ts`
Expected: FAIL (module introuvable).

- [ ] **Step 3: Implémenter `saml-user.ts`**

```ts
export interface SamlProfileInput {
  nameID: string;
  attributes: Record<string, unknown>;
}

export interface ResolvedSamlProfile {
  externalId: string;
  username: string;
  email: string | null;
  displayName: string;
  role: 'admin' | 'viewer';
}

/** First scalar value for an attribute (SAML attributes may be arrays). */
export function firstAttr(attributes: Record<string, unknown>, key: string): string | null {
  const v = attributes[key];
  if (Array.isArray(v)) return v.length ? String(v[0]) : null;
  if (v === undefined || v === null || v === '') return null;
  return String(v);
}

/** All values for an attribute, as strings. */
function allAttr(attributes: Record<string, unknown>, key: string): string[] {
  const v = attributes[key];
  if (Array.isArray(v)) return v.map(String);
  if (v === undefined || v === null || v === '') return [];
  return [String(v)];
}

export function resolveRole(
  attributes: Record<string, unknown>,
  settings: { attrRole: string | null; roleAdminValue: string | null; defaultRole: string },
): 'admin' | 'viewer' {
  if (settings.attrRole && settings.roleAdminValue) {
    const values = allAttr(attributes, settings.attrRole);
    if (values.includes(settings.roleAdminValue)) return 'admin';
  }
  return settings.defaultRole === 'admin' ? 'admin' : 'viewer';
}

export function buildSamlProfile(
  input: SamlProfileInput,
  settings: {
    attrUsername: string; attrEmail: string; attrDisplayName: string;
    attrRole: string | null; roleAdminValue: string | null; defaultRole: string;
  },
): ResolvedSamlProfile {
  const email = firstAttr(input.attributes, settings.attrEmail);
  const usernameAttr = firstAttr(input.attributes, settings.attrUsername);
  const username = usernameAttr || (email ? email.split('@')[0] : null) || input.nameID;
  const displayName = firstAttr(input.attributes, settings.attrDisplayName) || username;
  const role = resolveRole(input.attributes, settings);
  return { externalId: input.nameID, username, email, displayName, role };
}

/** Return `desired`, or `desired-2`, `desired-3`… until `isTaken` is false. */
export function disambiguateUsername(desired: string, isTaken: (candidate: string) => boolean): string {
  if (!isTaken(desired)) return desired;
  for (let i = 2; ; i++) {
    const candidate = `${desired}-${i}`;
    if (!isTaken(candidate)) return candidate;
  }
}
```

- [ ] **Step 4: Vérifier succès**

Run: `pnpm --filter @ts6/backend exec vitest run src/auth/saml/saml-user.test.ts`
Expected: PASS (tous les tests).

- [ ] **Step 5: Commit**

```bash
git add packages/backend/src/auth/saml/saml-user.ts packages/backend/src/auth/saml/saml-user.test.ts
git commit -m "feat(auth): pure SAML profile/role/username-disambiguation helpers"
```

---

### Task 4: Config SAML en mémoire + instance node-saml

Charge `SAMLSettings` depuis la DB, déchiffre les secrets, construit une instance `@node-saml/node-saml` singleton, et expose un rechargement à chaud. Ajoute la dépendance.

**Files:**
- Modify: `packages/backend/package.json` (dépendance)
- Create: `packages/backend/src/auth/saml/saml-config.ts`
- Test: `packages/backend/src/auth/saml/saml-config.test.ts`

**Interfaces:**
- Consumes: `PrismaClient`, `config` (`frontendUrl`), `decrypt`, `@node-saml/node-saml`.
- Produces:
  - `export function acsUrl(): string` et `export function spEntityIdDefault(): string` (dérivés de `config.frontendUrl`/backend base)
  - `export async function loadSamlRuntime(prisma: any): Promise<void>` (recharge le singleton depuis la DB)
  - `export function getSaml(): import('@node-saml/node-saml').SAML | null` (null si désactivé/incomplet)
  - `export function isSamlEnabled(): boolean`
  - `export function getRuntimeSettings(): SamlRuntimeSettings | null` (settings normalisés, secrets déchiffrés — usage interne routes)
  - `export function normalizeCert(pem: string): string` (pure : nettoie/encadre un certificat)

- [ ] **Step 1: Ajouter la dépendance**

Run: `pnpm --filter @ts6/backend add @node-saml/node-saml`
Expected: ajout dans `packages/backend/package.json`, install OK.

- [ ] **Step 2: Écrire le test de la partie pure**

Créer `packages/backend/src/auth/saml/saml-config.test.ts` (on teste `normalizeCert`, la seule logique pure ici ; le reste dépend de la DB/lib et sera couvert au niveau routes) :

```ts
import { describe, it, expect } from 'vitest';
import { normalizeCert } from './saml-config.js';

describe('normalizeCert', () => {
  it('laisse un PEM déjà encadré intact (hors espaces)', () => {
    const pem = '-----BEGIN CERTIFICATE-----\nAAAA\n-----END CERTIFICATE-----';
    expect(normalizeCert(pem)).toContain('BEGIN CERTIFICATE');
  });
  it('encadre un corps base64 nu', () => {
    const out = normalizeCert('AAAABBBBCCCC');
    expect(out.startsWith('-----BEGIN CERTIFICATE-----')).toBe(true);
    expect(out.trim().endsWith('-----END CERTIFICATE-----')).toBe(true);
  });
});
```

- [ ] **Step 3: Vérifier l'échec**

Run: `pnpm --filter @ts6/backend exec vitest run src/auth/saml/saml-config.test.ts`
Expected: FAIL (module introuvable).

- [ ] **Step 4: Implémenter `saml-config.ts`**

> Note : `@node-saml/node-saml` v5 — API utilisée : `new SAML(options)`, `getAuthorizeUrlAsync`, `validatePostResponseAsync`, `generateServiceProviderMetadata`, options `callbackUrl`/`entryPoint`/`issuer`/`idpCert`/`wantAssertionsSigned`/`validateInResponseTo`/`cacheProvider`. Vérifier les signatures exactes contre les types installés ; adapter si l'API diffère (tsc le signalera).

```ts
import { SAML } from '@node-saml/node-saml';
import { config } from '../../config.js';
import { decrypt } from '../../utils/crypto.js';

export interface SamlRuntimeSettings {
  enabled: boolean;
  spEntityId: string;
  autoProvision: boolean;
  defaultRole: string;
  attrUsername: string;
  attrEmail: string;
  attrDisplayName: string;
  attrRole: string | null;
  roleAdminValue: string | null;
}

let samlInstance: SAML | null = null;
let runtime: SamlRuntimeSettings | null = null;

/** Backend-visible ACS URL. FRONTEND_URL is the public origin; ACS is a backend path proxied under /api. */
export function acsUrl(): string {
  return `${config.frontendUrl.replace(/\/$/, '')}/api/auth/saml/acs`;
}

export function spEntityIdDefault(): string {
  return `${config.frontendUrl.replace(/\/$/, '')}/api/auth/saml/metadata`;
}

/** Wrap a bare base64 cert body in PEM markers; leave already-wrapped PEM as-is. */
export function normalizeCert(pem: string): string {
  const trimmed = (pem || '').trim();
  if (trimmed.includes('BEGIN CERTIFICATE')) return trimmed;
  const body = trimmed.replace(/\s+/g, '').match(/.{1,64}/g)?.join('\n') ?? '';
  return `-----BEGIN CERTIFICATE-----\n${body}\n-----END CERTIFICATE-----`;
}

function safeDecrypt(value: string | null): string | null {
  if (!value) return null;
  try { return decrypt(value); } catch { return value; }
}

/** Reload the in-memory SAML instance + normalized settings from the DB. Call at boot and after settings PUT. */
export async function loadSamlRuntime(prisma: any): Promise<void> {
  const s = await prisma.sAMLSettings.findFirst();
  if (!s || !s.enabled || !s.idpSsoUrl || !s.idpCertificate) {
    samlInstance = null;
    runtime = s ? {
      enabled: !!s.enabled, spEntityId: s.spEntityId || spEntityIdDefault(), autoProvision: s.autoProvision,
      defaultRole: s.defaultRole, attrUsername: s.attrUsername, attrEmail: s.attrEmail,
      attrDisplayName: s.attrDisplayName, attrRole: s.attrRole, roleAdminValue: s.roleAdminValue,
    } : null;
    return;
  }
  const cert = normalizeCert(safeDecrypt(s.idpCertificate) || '');
  const spEntityId = s.spEntityId || spEntityIdDefault();
  samlInstance = new SAML({
    callbackUrl: acsUrl(),
    entryPoint: s.idpSsoUrl,
    issuer: spEntityId,
    idpCert: cert,
    wantAssertionsSigned: true,
    audience: spEntityId,
    validateInResponseTo: 'ifPresent' as any,
  } as any);
  runtime = {
    enabled: true, spEntityId, autoProvision: s.autoProvision, defaultRole: s.defaultRole,
    attrUsername: s.attrUsername, attrEmail: s.attrEmail, attrDisplayName: s.attrDisplayName,
    attrRole: s.attrRole, roleAdminValue: s.roleAdminValue,
  };
}

export function getSaml(): SAML | null { return samlInstance; }
export function isSamlEnabled(): boolean { return !!runtime?.enabled; }
export function getRuntimeSettings(): SamlRuntimeSettings | null { return runtime; }
```

- [ ] **Step 5: Vérifier succès + compilation**

Run: `pnpm --filter @ts6/backend exec vitest run src/auth/saml/saml-config.test.ts && pnpm --filter @ts6/backend exec tsc --noEmit`
Expected: PASS (2 tests) + tsc clean.

- [ ] **Step 6: Commit**

```bash
git add packages/backend/package.json packages/backend/pnpm-lock.yaml packages/backend/src/auth/saml/saml-config.ts packages/backend/src/auth/saml/saml-config.test.ts
git commit -m "feat(auth): SAML runtime config + node-saml instance loader"
```
(Le lockfile peut être à la racine — inclure `pnpm-lock.yaml` là où il se trouve.)

---

### Task 5: Routes d'auth SAML (status/metadata/login/acs/exchange) + store de code

Implémente le flux SP-initiated complet et le store de code SSO à usage unique. Réutilise la logique pure (Task 3), la config (Task 4) et les helpers de session (Task 2).

**Files:**
- Create: `packages/backend/src/auth/saml/sso-code-store.ts`
- Test: `packages/backend/src/auth/saml/sso-code-store.test.ts`
- Create: `packages/backend/src/auth/saml/resolve-account.ts`
- Test: `packages/backend/src/auth/saml/resolve-account.test.ts`
- Create: `packages/backend/src/routes/saml-auth.routes.ts`
- Modify: `packages/backend/src/app.ts` (montage public + `loadSamlRuntime` au boot)

**Interfaces:**
- Consumes: `buildSamlProfile`/`disambiguateUsername` (Task 3), `getSaml`/`getRuntimeSettings`/`isSamlEnabled`/`acsUrl`/`spEntityIdDefault` (Task 4), `gateAfterPassword` (Task 2), `config.frontendUrl`.
- Produces:
  - `sso-code-store.ts` : `export function createSsoCode(userId: number): string`, `export function consumeSsoCode(code: string): number | null` (TTL 120 s, usage unique).
  - `resolve-account.ts` : `export async function resolveSamlAccount(prisma: any, profile: ResolvedSamlProfile, settings: { autoProvision: boolean }): Promise<{ user: any } | { error: 'not_provisioned' | 'disabled' }>`
  - Router `samlAuthRoutes` monté à `/api/auth/saml`.

- [ ] **Step 1: Écrire les tests du store de code**

Créer `packages/backend/src/auth/saml/sso-code-store.test.ts` :

```ts
import { describe, it, expect } from 'vitest';
import { createSsoCode, consumeSsoCode } from './sso-code-store.js';

describe('sso-code-store', () => {
  it('crée puis consomme un code une seule fois', () => {
    const code = createSsoCode(42);
    expect(consumeSsoCode(code)).toBe(42);
    expect(consumeSsoCode(code)).toBeNull(); // usage unique
  });
  it('retourne null pour un code inconnu', () => {
    expect(consumeSsoCode('nope')).toBeNull();
  });
});
```

- [ ] **Step 2: Vérifier l'échec puis implémenter le store**

Run (échec attendu, module manquant): `pnpm --filter @ts6/backend exec vitest run src/auth/saml/sso-code-store.test.ts`

Créer `packages/backend/src/auth/saml/sso-code-store.ts` :

```ts
import crypto from 'crypto';

const TTL_MS = 120_000;
const codes = new Map<string, { userId: number; expiresAt: number }>();

/** Mint an opaque one-time code bound to a user id. */
export function createSsoCode(userId: number): string {
  const code = crypto.randomBytes(32).toString('hex');
  codes.set(code, { userId, expiresAt: Date.now() + TTL_MS });
  return code;
}

/** Consume a code: returns the user id once, then never again. */
export function consumeSsoCode(code: string): number | null {
  const entry = codes.get(code);
  if (!entry) return null;
  codes.delete(code);
  if (entry.expiresAt < Date.now()) return null;
  return entry.userId;
}
```

Run (succès): `pnpm --filter @ts6/backend exec vitest run src/auth/saml/sso-code-store.test.ts`
Expected: PASS.

> Note : `Date.now()` est interdit dans les workflows mais **autorisé** dans le code applicatif normal — c'est ici du code d'exécution standard.

- [ ] **Step 3: Écrire les tests de résolution de compte**

Créer `packages/backend/src/auth/saml/resolve-account.test.ts` (Prisma mocké in-memory) :

```ts
import { describe, it, expect } from 'vitest';
import { resolveSamlAccount } from './resolve-account.js';
import type { ResolvedSamlProfile } from './saml-user.js';

function makePrisma(initial: any[] = []) {
  const users = [...initial];
  let seq = users.length;
  return {
    users,
    user: {
      findUnique: async ({ where }: any) => {
        if (where.authProvider_externalId) {
          const { authProvider, externalId } = where.authProvider_externalId;
          return users.find((u) => u.authProvider === authProvider && u.externalId === externalId) || null;
        }
        if (where.username) return users.find((u) => u.username === where.username) || null;
        return null;
      },
      findMany: async ({ where }: any) => {
        const prefix = where?.username?.startsWith ?? '';
        return users.filter((u) => u.username.startsWith(prefix)).map((u) => ({ username: u.username }));
      },
      create: async ({ data }: any) => { const u = { id: ++seq, ...data }; users.push(u); return u; },
      update: async ({ where, data }: any) => {
        const u = users.find((x) => x.id === where.id); Object.assign(u, data); return u;
      },
    },
  };
}

const profile: ResolvedSamlProfile = { externalId: 'uid-1', username: 'bob', email: 'b@x.io', displayName: 'Bob', role: 'viewer' };

describe('resolveSamlAccount', () => {
  it('crée un compte JIT quand autoProvision=true', async () => {
    const prisma = makePrisma();
    const r = await resolveSamlAccount(prisma, profile, { autoProvision: true });
    expect('user' in r && r.user.username).toBe('bob');
    expect(prisma.users).toHaveLength(1);
    expect(prisma.users[0].authProvider).toBe('saml');
    expect(prisma.users[0].passwordHash).toBeNull();
  });

  it('échoue quand autoProvision=false et compte inconnu', async () => {
    const prisma = makePrisma();
    const r = await resolveSamlAccount(prisma, profile, { autoProvision: false });
    expect(r).toEqual({ error: 'not_provisioned' });
    expect(prisma.users).toHaveLength(0);
  });

  it('recalcule le rôle et le displayName à chaque login', async () => {
    const prisma = makePrisma([{ id: 1, authProvider: 'saml', externalId: 'uid-1', username: 'bob', displayName: 'Old', role: 'viewer', enabled: true, passwordHash: null }]);
    const r = await resolveSamlAccount(prisma, { ...profile, role: 'admin', displayName: 'New' }, { autoProvision: false });
    expect('user' in r && r.user.role).toBe('admin');
    expect(prisma.users[0].displayName).toBe('New');
  });

  it('refuse un compte désactivé', async () => {
    const prisma = makePrisma([{ id: 1, authProvider: 'saml', externalId: 'uid-1', username: 'bob', role: 'viewer', enabled: false, passwordHash: null }]);
    const r = await resolveSamlAccount(prisma, profile, { autoProvision: false });
    expect(r).toEqual({ error: 'disabled' });
  });

  it('désambiguïse le username en cas de collision à la création', async () => {
    const prisma = makePrisma([{ id: 1, authProvider: 'local', externalId: null, username: 'bob', role: 'viewer', enabled: true, passwordHash: 'x' }]);
    const r = await resolveSamlAccount(prisma, profile, { autoProvision: true });
    expect('user' in r && r.user.username).toBe('bob-2');
  });
});
```

- [ ] **Step 4: Vérifier l'échec puis implémenter `resolve-account.ts`**

Run (échec attendu): `pnpm --filter @ts6/backend exec vitest run src/auth/saml/resolve-account.test.ts`

Créer `packages/backend/src/auth/saml/resolve-account.ts` :

```ts
import type { ResolvedSamlProfile } from './saml-user.js';
import { disambiguateUsername } from './saml-user.js';

/**
 * Find the SAML user by (authProvider, externalId); create it JIT when allowed,
 * otherwise fail. Existing users get displayName + role re-synced (IdP authoritative).
 */
export async function resolveSamlAccount(
  prisma: any,
  profile: ResolvedSamlProfile,
  settings: { autoProvision: boolean },
): Promise<{ user: any } | { error: 'not_provisioned' | 'disabled' }> {
  const existing = await prisma.user.findUnique({
    where: { authProvider_externalId: { authProvider: 'saml', externalId: profile.externalId } },
  });

  if (existing) {
    if (!existing.enabled) return { error: 'disabled' };
    const user = await prisma.user.update({
      where: { id: existing.id },
      data: { displayName: profile.displayName, role: profile.role },
    });
    return { user };
  }

  if (!settings.autoProvision) return { error: 'not_provisioned' };

  // Resolve a free username (externalId is the identity key, not the username).
  // Fetch existing usernames sharing the prefix in one query, then let the pure
  // disambiguator pick the first free candidate (name, name-2, name-3…).
  const clashes = await prisma.user.findMany({
    where: { username: { startsWith: profile.username } },
    select: { username: true },
  });
  const taken = new Set<string>(clashes.map((c: any) => c.username));
  const username = disambiguateUsername(profile.username, (c) => taken.has(c));

  const user = await prisma.user.create({
    data: {
      username,
      displayName: profile.displayName,
      role: profile.role,
      authProvider: 'saml',
      externalId: profile.externalId,
      passwordHash: null,
      enabled: true,
    },
  });
  return { user };
}
```

Run (succès): `pnpm --filter @ts6/backend exec vitest run src/auth/saml/resolve-account.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 5: Implémenter le router `saml-auth.routes.ts`**

Créer `packages/backend/src/routes/saml-auth.routes.ts` :

```ts
import { Router, Request, Response } from 'express';
import rateLimit from 'express-rate-limit';
import { config } from '../config.js';
import { AppError } from '../middleware/error-handler.js';
import { getSaml, getRuntimeSettings, isSamlEnabled, acsUrl, spEntityIdDefault } from '../auth/saml/saml-config.js';
import { buildSamlProfile } from '../auth/saml/saml-user.js';
import { resolveSamlAccount } from '../auth/saml/resolve-account.js';
import { createSsoCode, consumeSsoCode } from '../auth/saml/sso-code-store.js';
import { gateAfterPassword } from '../auth/session.js';

export const samlAuthRoutes: Router = Router();

const acsLimiter = rateLimit({ windowMs: 60_000, max: 20, standardHeaders: true, legacyHeaders: false });
const loginBase = () => config.frontendUrl.replace(/\/$/, '');

samlAuthRoutes.get('/status', (_req, res) => {
  res.json({ enabled: isSamlEnabled() });
});

samlAuthRoutes.get('/metadata', (_req, res, next) => {
  try {
    const saml = getSaml();
    if (!saml) throw new AppError(404, 'SAML disabled');
    const xml = saml.generateServiceProviderMetadata(null, null);
    res.type('application/xml').send(xml);
  } catch (err) { next(err); }
});

samlAuthRoutes.get('/login', async (req: Request, res: Response, next) => {
  try {
    const saml = getSaml();
    if (!saml) throw new AppError(404, 'SAML disabled');
    const url = await saml.getAuthorizeUrlAsync('', undefined as any, {} as any);
    res.redirect(url);
  } catch (err) { next(err); }
});

samlAuthRoutes.post('/acs', acsLimiter, async (req: Request, res: Response, next) => {
  try {
    const saml = getSaml();
    const rt = getRuntimeSettings();
    if (!saml || !rt) throw new AppError(404, 'SAML disabled');
    const prisma = req.app.locals.prisma;

    const { profile } = await saml.validatePostResponseAsync(req.body);
    if (!profile?.nameID) throw new AppError(401, 'Invalid SAML assertion');

    const resolved = buildSamlProfile(
      { nameID: String(profile.nameID), attributes: (profile.attributes as any) || (profile as any) },
      rt,
    );
    const account = await resolveSamlAccount(prisma, resolved, { autoProvision: rt.autoProvision });
    if ('error' in account) {
      return res.redirect(`${loginBase()}/login/sso?error=${account.error}`);
    }
    const code = createSsoCode(account.user.id);
    res.redirect(`${loginBase()}/login/sso?code=${code}`);
  } catch (err) {
    // Never leak validation details to the browser; log server-side, redirect with a generic error.
    console.error('[SAML] ACS error:', (err as any)?.message);
    res.redirect(`${loginBase()}/login/sso?error=saml_failed`);
  }
});

const exchangeLimiter = rateLimit({ windowMs: 60_000, max: 30, standardHeaders: true, legacyHeaders: false });
samlAuthRoutes.post('/exchange', exchangeLimiter, async (req: Request, res: Response, next) => {
  try {
    const { code } = req.body;
    if (!code) throw new AppError(400, 'Code required');
    const userId = consumeSsoCode(String(code));
    if (!userId) throw new AppError(401, 'Invalid or expired code');
    const prisma = req.app.locals.prisma;
    const user = await prisma.user.findUnique({ where: { id: userId } });
    if (!user || !user.enabled) throw new AppError(401, 'Account unavailable');
    const result = await gateAfterPassword(prisma, user);
    res.json(result);
  } catch (err) { next(err); }
});
```

> `acsUrl`/`spEntityIdDefault` sont importés pour cohérence de config ; s'ils ne sont pas utilisés directement ici, retirer l'import (tsc `noUnusedLocals` le signalera). Vérifier la signature exacte de `getAuthorizeUrlAsync` / `validatePostResponseAsync` / `generateServiceProviderMetadata` contre les types de `@node-saml/node-saml` installés et ajuster les arguments.

- [ ] **Step 6: Monter les routes + charger la config au boot dans `app.ts`**

Dans `packages/backend/src/app.ts`, importer et monter **avant** `app.use('/api', authMiddleware)` (près de la ligne 95) :

```ts
import { samlAuthRoutes } from './routes/saml-auth.routes.js';
import { loadSamlRuntime } from './auth/saml/saml-config.js';
// ...
app.use('/api/auth/saml', samlAuthRoutes);
```

S'assurer que `express.json()` (ou `urlencoded`) est actif pour parser le POST de l'ACS (la SAMLResponse arrive en `application/x-www-form-urlencoded`) : vérifier qu'un `app.use(express.urlencoded({ extended: false }))` existe ; sinon l'ajouter avant le montage des routes. Charger la config SAML au démarrage — à l'endroit où les autres bridges/settings sont initialisés (après création de `prisma` dans `app.locals`) :

```ts
await loadSamlRuntime(prisma);
```

- [ ] **Step 7: Compiler + suite complète**

Run: `pnpm --filter @ts6/backend exec tsc --noEmit && pnpm --filter @ts6/backend exec vitest run`
Expected: PASS (compilation clean, tous les tests verts).

- [ ] **Step 8: Commit**

```bash
git add packages/backend/src/auth/saml packages/backend/src/routes/saml-auth.routes.ts packages/backend/src/app.ts
git commit -m "feat(auth): SP-initiated SAML routes with one-time code handoff"
```

---

### Task 6: API admin des réglages SAML + client frontend

Expose GET/PUT des `SAMLSettings` (admin), masque les secrets, parse la metadata IdP fournie, recharge la config à chaud. Ajoute le client API frontend.

**Files:**
- Create: `packages/backend/src/routes/saml.routes.ts`
- Test: `packages/backend/src/routes/saml-settings.test.ts`
- Modify: `packages/backend/src/app.ts` (montage protégé)
- Create: `packages/frontend/src/api/saml.api.ts`

**Interfaces:**
- Consumes: `encrypt`/`decrypt`, `loadSamlRuntime`, `acsUrl`, `spEntityIdDefault`, `@node-saml/node-saml` (parse metadata).
- Produces:
  - `export function sanitizeSamlSettingsInput(body: any): Record<string, unknown>` (pure : coercions/normalisation avant persistance)
  - Router `samlRoutes` monté à `/api/saml` (admin).
  - Frontend `samlApi.settings()` / `samlApi.updateSettings(data)`.

- [ ] **Step 1: Écrire le test de normalisation**

Créer `packages/backend/src/routes/saml-settings.test.ts` :

```ts
import { describe, it, expect } from 'vitest';
import { sanitizeSamlSettingsInput } from './saml.routes.js';

describe('sanitizeSamlSettingsInput', () => {
  it('coerce enabled/autoProvision en booléen', () => {
    const out = sanitizeSamlSettingsInput({ enabled: 'true', autoProvision: 0 });
    expect(out.enabled).toBe(true);
    expect(out.autoProvision).toBe(false);
  });
  it('normalise defaultRole invalide vers viewer', () => {
    expect(sanitizeSamlSettingsInput({ defaultRole: 'root' }).defaultRole).toBe('viewer');
    expect(sanitizeSamlSettingsInput({ defaultRole: 'admin' }).defaultRole).toBe('admin');
  });
  it('convertit les chaînes vides en null pour les champs optionnels', () => {
    const out = sanitizeSamlSettingsInput({ attrRole: '', roleAdminValue: '' });
    expect(out.attrRole).toBeNull();
    expect(out.roleAdminValue).toBeNull();
  });
  it('ignore les clés inconnues', () => {
    expect('hacker' in sanitizeSamlSettingsInput({ hacker: 1 })).toBe(false);
  });
});
```

- [ ] **Step 2: Vérifier l'échec puis implémenter `saml.routes.ts`**

Run (échec attendu): `pnpm --filter @ts6/backend exec vitest run src/routes/saml-settings.test.ts`

Créer `packages/backend/src/routes/saml.routes.ts` :

```ts
import { Router, Request, Response } from 'express';
import { encrypt } from '../utils/crypto.js';
import { loadSamlRuntime, acsUrl, spEntityIdDefault, normalizeCert } from '../auth/saml/saml-config.js';

export const samlRoutes: Router = Router();

const OPTIONAL_STRINGS = ['idpMetadataUrl', 'idpEntityId', 'idpSsoUrl', 'spEntityId', 'attrRole', 'roleAdminValue'];
const REQUIRED_STRINGS = ['attrUsername', 'attrEmail', 'attrDisplayName'];

/** Coerce + normalize the settings PUT body; drop unknown keys. Pure. */
export function sanitizeSamlSettingsInput(body: any): Record<string, unknown> {
  const out: Record<string, unknown> = {};
  if (body.enabled !== undefined) out.enabled = !!body.enabled && body.enabled !== 'false';
  if (body.autoProvision !== undefined) out.autoProvision = !!body.autoProvision && body.autoProvision !== 'false';
  if (body.defaultRole !== undefined) out.defaultRole = body.defaultRole === 'admin' ? 'admin' : 'viewer';
  for (const k of OPTIONAL_STRINGS) if (body[k] !== undefined) out[k] = body[k] ? String(body[k]) : null;
  for (const k of REQUIRED_STRINGS) if (body[k] !== undefined && body[k]) out[k] = String(body[k]);
  return out;
}

async function getOrCreate(prisma: any) {
  return (await prisma.sAMLSettings.findFirst()) || (await prisma.sAMLSettings.create({ data: {} }));
}

samlRoutes.get('/settings', async (req: Request, res: Response, next) => {
  try {
    const prisma = req.app.locals.prisma;
    const s = await getOrCreate(prisma);
    res.json({
      enabled: s.enabled,
      idpMetadataUrl: s.idpMetadataUrl,
      idpEntityId: s.idpEntityId,
      idpSsoUrl: s.idpSsoUrl,
      hasIdpMetadataXml: !!s.idpMetadataXml,
      hasIdpCertificate: !!s.idpCertificate,
      spEntityId: s.spEntityId || spEntityIdDefault(),
      autoProvision: s.autoProvision,
      defaultRole: s.defaultRole,
      attrUsername: s.attrUsername, attrEmail: s.attrEmail, attrDisplayName: s.attrDisplayName,
      attrRole: s.attrRole, roleAdminValue: s.roleAdminValue,
      // Read-only helpers for the admin to configure the IdP:
      spMetadataUrl: spEntityIdDefault(),
      acsUrl: acsUrl(),
    });
  } catch (err) { next(err); }
});

samlRoutes.put('/settings', async (req: Request, res: Response, next) => {
  try {
    const prisma = req.app.locals.prisma;
    const s = await getOrCreate(prisma);
    const data = sanitizeSamlSettingsInput(req.body);

    // Secrets: accept raw values, store encrypted; empty string clears.
    if (req.body.idpMetadataXml !== undefined) {
      data.idpMetadataXml = req.body.idpMetadataXml ? encrypt(String(req.body.idpMetadataXml)) : null;
    }
    if (req.body.idpCertificate !== undefined) {
      data.idpCertificate = req.body.idpCertificate ? encrypt(normalizeCert(String(req.body.idpCertificate))) : null;
    }

    await prisma.sAMLSettings.update({ where: { id: s.id }, data });
    await loadSamlRuntime(prisma); // hot reload
    res.json({ ok: true });
  } catch (err) { next(err); }
});
```

> v1 : le certificat/SSO URL/entityID de l'IdP sont saisis manuellement par l'admin (copiés depuis Authentik). Le parsing automatique d'une metadata URL/XML pour pré-remplir ces champs est une amélioration ultérieure ; les champs `idpSsoUrl`/`idpCertificate` sont donc requis pour activer SAML (cf. `loadSamlRuntime`).

Run (succès): `pnpm --filter @ts6/backend exec vitest run src/routes/saml-settings.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 3: Monter le router admin dans `app.ts`**

Après `app.use('/api', authMiddleware)` (avec les autres routes protégées, ~ligne 129), en réservant aux admins :

```ts
import { samlRoutes } from './routes/saml.routes.js';
import { requireRole } from './middleware/rbac.js';
// ...
app.use('/api/saml', requireRole('admin'), samlRoutes);
```

- [ ] **Step 4: Client API frontend**

Créer `packages/frontend/src/api/saml.api.ts` (pattern `discord.api.ts`) :

```ts
import { api } from './client';

export interface SamlSettings {
  enabled: boolean;
  idpMetadataUrl: string | null;
  idpEntityId: string | null;
  idpSsoUrl: string | null;
  hasIdpMetadataXml: boolean;
  hasIdpCertificate: boolean;
  spEntityId: string;
  autoProvision: boolean;
  defaultRole: 'admin' | 'viewer';
  attrUsername: string;
  attrEmail: string;
  attrDisplayName: string;
  attrRole: string | null;
  roleAdminValue: string | null;
  spMetadataUrl: string;
  acsUrl: string;
}

export const samlApi = {
  settings: (): Promise<SamlSettings> => api.get('/saml/settings').then((r) => r.data),
  updateSettings: (data: Partial<SamlSettings> & { idpMetadataXml?: string; idpCertificate?: string }) =>
    api.put('/saml/settings', data).then((r) => r.data),
};
```

- [ ] **Step 5: Compiler backend + frontend**

Run: `pnpm --filter @ts6/backend exec tsc --noEmit && pnpm --filter @ts6/frontend exec tsc --noEmit`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add packages/backend/src/routes/saml.routes.ts packages/backend/src/routes/saml-settings.test.ts packages/backend/src/app.ts packages/frontend/src/api/saml.api.ts
git commit -m "feat(auth): admin SAML settings API + frontend client"
```

---

### Task 7: Frontend — bouton SSO + callback d'échange

Ajoute le bouton « Se connecter via SSO » sur la page de login (si SAML activé) et l'écran de callback `/login/sso` qui échange le code et poursuit le flux MFA.

**Files:**
- Modify: `packages/frontend/src/api/auth.api.ts` (ajouts `samlStatus`, `samlExchange`)
- Modify: `packages/frontend/src/pages/Login.tsx` (bouton SSO)
- Create: `packages/frontend/src/pages/SsoCallback.tsx`
- Modify: `packages/frontend/src/App.tsx` (route publique `/login/sso`)

**Interfaces:**
- Consumes: `samlApi` non requis ici ; utilise `auth.api`. `useAuthStore.setAuth`.
- Produces: `authApi.samlStatus(): Promise<{ enabled: boolean }>`, `authApi.samlExchange(code: string): Promise<any>` (même forme que `login` : session ou `{ mfaRequired, mfaToken }`).

- [ ] **Step 1: Étendre `auth.api.ts`**

Dans `packages/frontend/src/api/auth.api.ts`, ajouter à l'objet `authApi` :

```ts
  samlStatus: (): Promise<{ enabled: boolean }> => api.get('/auth/saml/status').then((r) => r.data),
  samlExchange: (code: string) => api.post('/auth/saml/exchange', { code }).then((r) => r.data),
```

- [ ] **Step 2: Bouton SSO dans `Login.tsx`**

Ajouter un state `samlEnabled` (via `authApi.samlStatus()` au montage) et, sous le formulaire de mot de passe (état `'password'`), un bouton conditionnel. Construire l'URL absolue de l'API (base identique à l'instance axios) :

```tsx
// en haut du composant
const [samlEnabled, setSamlEnabled] = useState(false);
useEffect(() => { authApi.samlStatus().then((s) => setSamlEnabled(s.enabled)).catch(() => {}); }, []);

// URL absolue backend (l'axios baseURL vaut p.ex. "/api" ; réutiliser la même origine)
const ssoLoginUrl = `${api.defaults.baseURL?.replace(/\/$/, '')}/auth/saml/login`;

// dans le rendu de l'étape 'password', après le bouton de connexion :
{samlEnabled && (
  <>
    <div className="my-3 flex items-center gap-2 text-xs text-muted-foreground">
      <div className="h-px flex-1 bg-border" /> {t('login.or')} <div className="h-px flex-1 bg-border" />
    </div>
    <a href={ssoLoginUrl} className="block">
      <Button type="button" variant="outline" className="w-full">{t('login.sso')}</Button>
    </a>
  </>
)}
```
(Importer `api` depuis `../api/client` si nécessaire pour `api.defaults.baseURL`, et `authApi`.)

- [ ] **Step 3: Écran de callback `SsoCallback.tsx`**

Créer `packages/frontend/src/pages/SsoCallback.tsx` :

```tsx
import { useEffect, useState } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { authApi } from '../api/auth.api';
import { useAuthStore } from '../stores/auth.store';

export default function SsoCallback() {
  const [params] = useSearchParams();
  const navigate = useNavigate();
  const { t } = useTranslation();
  const setAuth = useAuthStore((s) => s.setAuth);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const err = params.get('error');
    if (err) { setError(err); return; }
    const code = params.get('code');
    if (!code) { setError('missing_code'); return; }
    authApi.samlExchange(code)
      .then((res) => {
        if (res.accessToken) {
          setAuth(res.accessToken, res.refreshToken, res.user);
          navigate('/dashboard', { replace: true });
        } else {
          // MFA required: hand off to the login page's MFA step via router state.
          navigate('/login', { replace: true, state: { mfa: res } });
        }
      })
      .catch(() => setError('exchange_failed'));
  }, [params, navigate, setAuth, t]);

  return (
    <div className="flex min-h-screen items-center justify-center p-4 text-sm">
      {error
        ? <div className="space-y-2 text-center">
            <p className="text-destructive">{t(`login.ssoError.${error}`, t('login.ssoError.generic'))}</p>
            <a className="text-primary underline" href="/login">{t('login.backToLogin')}</a>
          </div>
        : <p className="text-muted-foreground">{t('login.ssoProcessing')}</p>}
    </div>
  );
}
```

> Le flux MFA post-SSO passe l'objet `{ mfaRequired, mfaToken }` à `/login` via `state`. `Login.tsx` doit lire `location.state?.mfa` au montage et, le cas échéant, entrer directement dans son étape MFA existante (`'code'`/`'setup'`) en réutilisant `mfaToken`. Ajouter ce court branchement dans `Login.tsx` (lecture de `useLocation().state`).

- [ ] **Step 4: Route publique dans `App.tsx`**

Ajouter, à côté de la route `/login` (hors `AppLayout`, publique) :

```tsx
<Route path="/login/sso" element={<SsoCallback />} />
```
(importer `SsoCallback`.)

- [ ] **Step 5: Compiler + build frontend**

Run: `pnpm --filter @ts6/frontend exec tsc --noEmit`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add packages/frontend/src/api/auth.api.ts packages/frontend/src/pages/Login.tsx packages/frontend/src/pages/SsoCallback.tsx packages/frontend/src/App.tsx
git commit -m "feat(web): SSO login button and code-exchange callback"
```

---

### Task 8: Onglet Paramètres « SSO / SAML » + i18n

Ajoute l'onglet admin de configuration SAML et toutes les clés i18n (bouton login, callback, onglet) dans les 5 langues.

**Files:**
- Modify: `packages/frontend/src/pages/Settings.tsx` (nouvel onglet)
- Modify: fichiers i18n `packages/frontend/src/i18n/locales/{fr,en,de,es,it}.json`

**Interfaces:**
- Consumes: `samlApi` (Task 6), pattern `DiscordTab`, TanStack Query.
- Produces: onglet `saml` dans `Settings.tsx`.

- [ ] **Step 1: Repérer la structure i18n et le pattern d'onglet**

Run: `rg "\"discord\"" packages/frontend/src/i18n/locales/en.json` et ouvrir `Settings.tsx` autour de `value="discord"` (TabsTrigger ~ligne 47, TabsContent ~ligne 75, `DiscordTab`).

- [ ] **Step 2: Ajouter les clés i18n (5 langues)**

Sous `settings.tabs`, ajouter `saml`. Sous une nouvelle section `settings.saml`, ajouter : `enable`, `autoProvision`, `defaultRole`, `idpSsoUrl`, `idpCertificate`, `idpEntityId`, `attrUsername`, `attrEmail`, `attrDisplayName`, `attrRole`, `roleAdminValue`, `spInfo` (aide), `spMetadataUrl`, `acsUrl`, `saved`. Et sous `login` : `or`, `sso`, `ssoProcessing`, `backToLogin`, `ssoError.generic`, `ssoError.not_provisioned`, `ssoError.saml_failed`, `ssoError.exchange_failed`, `ssoError.missing_code`.

Valeurs FR (traduire fidèlement dans en/de/es/it, diacritiques correctes) :
- `settings.tabs.saml` = "SSO / SAML"
- `settings.saml.enable` = "Activer l'authentification SSO (SAML)"
- `settings.saml.autoProvision` = "Provisionnement automatique des comptes authentifiés via SSO"
- `settings.saml.defaultRole` = "Rôle par défaut des comptes SSO"
- `settings.saml.idpSsoUrl` = "URL SSO de l'IdP"
- `settings.saml.idpCertificate` = "Certificat de signature de l'IdP"
- `settings.saml.idpEntityId` = "Entity ID de l'IdP"
- `settings.saml.attrUsername` = "Attribut : nom d'utilisateur"
- `settings.saml.attrEmail` = "Attribut : e-mail"
- `settings.saml.attrDisplayName` = "Attribut : nom affiché"
- `settings.saml.attrRole` = "Attribut : rôle / groupe"
- `settings.saml.roleAdminValue` = "Valeur accordant le rôle admin"
- `settings.saml.spInfo` = "À renseigner côté IdP (Authentik) :"
- `settings.saml.spMetadataUrl` = "URL de metadata du SP"
- `settings.saml.acsUrl` = "URL ACS (Assertion Consumer Service)"
- `settings.saml.saved` = "Réglages SSO enregistrés"
- `login.or` = "ou"
- `login.sso` = "Se connecter via SSO"
- `login.ssoProcessing` = "Connexion en cours…"
- `login.backToLogin` = "Retour à la connexion"
- `login.ssoError.generic` = "Échec de la connexion SSO"
- `login.ssoError.not_provisioned` = "Compte non provisionné. Contactez un administrateur."
- `login.ssoError.saml_failed` = "L'authentification SAML a échoué"
- `login.ssoError.exchange_failed` = "Session SSO invalide ou expirée"
- `login.ssoError.missing_code` = "Réponse SSO invalide"

- [ ] **Step 3: Ajouter le `SamlTab` + trigger/content**

Dans `Settings.tsx` : nouveau `TabsTrigger value="saml"` (admin, à côté de `discord`) et `TabsContent value="saml"`. Composant `SamlTab` sur le modèle de `DiscordTab` (chargement via `useQuery(['saml-settings'], samlApi.settings)`, form local, mutation `samlApi.updateSettings`, toast `t('settings.saml.saved')`). Contenu :
- Toggle `enabled`.
- Bloc lecture seule affichant `spMetadataUrl` et `acsUrl` (avec `t('settings.saml.spInfo')`), champs copiables.
- Inputs : `idpEntityId`, `idpSsoUrl`, `idpCertificate` (textarea), mappings d'attributs, `attrRole`, `roleAdminValue`.
- Case `autoProvision` + sélecteur `defaultRole` (viewer/admin).
- Bouton Enregistrer.

Réutiliser les composants UI déjà importés dans `Settings.tsx` (`Switch`, `Label`, `Input`, `Button`, éventuellement `Textarea`/`Select` si présents ; sinon utiliser un `<textarea>`/`<select>` stylé comme ailleurs). Toutes les chaînes via `t('settings.saml.*')`.

- [ ] **Step 4: Compiler + vérif visuelle**

Run: `pnpm --filter @ts6/frontend exec tsc --noEmit`
Expected: PASS. Puis vérifier visuellement l'onglet (admin) : affichage des URLs SP, saisie IdP, toggle, sauvegarde → toast.

- [ ] **Step 5: Commit**

```bash
git add packages/frontend/src/pages/Settings.tsx packages/frontend/src/i18n/locales
git commit -m "feat(web): SAML settings tab and SSO i18n (5 languages)"
```

---

### Task 9: Vérification finale

**Files:** aucun (vérification).

- [ ] **Step 1: Backend**

Run: `pnpm --filter @ts6/backend exec tsc --noEmit && pnpm --filter @ts6/backend exec vitest run`
Expected: PASS.

- [ ] **Step 2: Frontend**

Run: `pnpm --filter @ts6/frontend exec tsc --noEmit`
Expected: PASS.

- [ ] **Step 3: Test manuel bout-en-bout (avec Authentik)**

Dans Authentik : créer un provider SAML pointant vers l'ACS URL affichée, récupérer l'URL SSO + le certificat de signature, les coller dans l'onglet SSO/SAML, activer. Depuis la page de login : bouton « Se connecter via SSO » → login Authentik → retour app. Vérifier : création JIT (autoProvision on), rôle admin si groupe mappé, échec propre si autoProvision off pour un compte inconnu, et que le login local classique fonctionne toujours.

---

## Notes d'exécution

- Vérifier les signatures exactes de `@node-saml/node-saml` v5 (`getAuthorizeUrlAsync`, `validatePostResponseAsync`, `generateServiceProviderMetadata`, options) contre les types installés ; ajuster les appels si nécessaire (tsc/`noUnusedLocals` guident).
- Le nom du modèle Prisma `SAMLSettings` s'accède via `prisma.sAMLSettings` (Prisma camelCase avec acronyme). Vérifier le nom généré exact après `migrate dev` et adapter les accès si Prisma génère `prisma.samlSettings`.
- Confirmer que `express.urlencoded` est monté (POST ACS `application/x-www-form-urlencoded`) avant de tester l'ACS.
- `defaultRole` : type `'admin' | 'viewer'` — cohérent avec `UserRole` de `@ts6/common`.
